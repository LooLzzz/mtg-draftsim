const validateLoginInput = require('./login')
const validateSignupInput = require('./signup')
const validateToken = require('./token')

module.exports = {
    validateLoginInput,
    validateSignupInput,
    validateToken,
}